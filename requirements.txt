# Core dependencies for RobotPC teleoperation
# Versions based on lerobot_piper/pyproject.toml

# Core packages
draccus>=0.9.3
pyzmq>=26.2.1

# Piper robot control dependencies
python-can>=3.3.4  # Required by piper_sdk
# piper-sdk>=0.3.2  # Installed locally from piper_sdk/ with pip install -e .